# Cargo audit configuration
# https://github.com/rustsec/cargo-audit/blob/main/cargo-audit.tom.example

[advisories]
ignore = [
    # glib 0.18.5 — unsoundness in VariantStrIter (Linux GTK only, not used on Windows)
    "RUSTSEC-2024-0429",
    # unic-ucd-ident 0.9.0 — unmaintained (transitive dep via urlpattern → Tauri)
    "RUSTSEC-2025-0080",
    # unic-ucd-version 0.9.0 — unmaintained (transitive dep via urlpattern → Tauri)
    "RUSTSEC-2025-0100",
]
